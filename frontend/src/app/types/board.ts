export type Board = {
    created_at: string;
    description: string;
    id: number; // Ensure this matches the type provided by your API
    name: string;
    role: string;
    updated_at: string;
  };